# OpenAI

Implemented using `spring-ai-openai-spring-boot-starter`.

- Requires `OPENAI_API_KEY`.
- Classified as `CONFIDENTIAL` privacy maximum.
- Supports `CHAT`, `STRUCTURED_OUTPUT`, and `TOOL_CALLING`.
