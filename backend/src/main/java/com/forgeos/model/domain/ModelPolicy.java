package com.forgeos.model.domain;

public enum ModelPolicy {
    DEFAULT,
    CHEAP,
    LOCAL_ONLY,
    PREMIUM,
    PRIVACY_FIRST,
    LATENCY_FIRST,
    QUALITY_FIRST,
    BUDGET_LIMITED,
    MOCK_ONLY // Used for testing
}
